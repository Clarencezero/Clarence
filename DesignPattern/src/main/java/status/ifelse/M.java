package status.ifelse;

import status.advance.GumballMachine;

public class M {
    public static void main(String[] args) {
        GumballMachine gumballMachine = new GumballMachine(2);
        gumballMachine.ejectQuarter();
        gumballMachine.releaesBall();
    }
}
